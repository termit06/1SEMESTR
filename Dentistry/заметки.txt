папки внутри прописываются строчными символами
папки:

      * img - изображения
      * font - шрифты
      * scss / sass - предпроцессоры
      * css - стили

в файле index.html - присутствует разметка для прижатия подвала к нижней части сайта, в стилях всё прописано

подключение normalize.css к index.html, для обнуления стилей браузера пользователя

Добавляйте в .gitignore файл style.css.map, так как это компилятор от sass, и он не нужен в репозитории